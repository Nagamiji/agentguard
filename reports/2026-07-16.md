# Engineering report — 2026-07-16

**Sprint 0.** Three cycles: (1) resolve the interrupted rebase + build the merge gate,
(2) BE-02 agent-registry design, (3) BE-02 implementation. Two PRs open, both 9/9 green,
both awaiting the human merge gate.

---

## 1. What changed

**PR #1 — `ci: establish the main-branch merge gate`** (open, 9/9 green, unmerged)
- Rebase resolved. The staged "resolution" still contained a `>>>>>>>` conflict marker and
  would have been committed as-is. `origin/main`'s README was the stub `# agentguard`;
  keeping our full docs lost nothing. History linear, no commits lost.
- `.github/workflows/ci.yml` — one required check, `gate`, fanning in on ruff, mypy --strict,
  unit pytest, integration pytest (Postgres + RLS), gitleaks + pip-audit, Docker build,
  terraform fmt/validate, PR-title style.
- `security.yml` (weekly scans), `release.yml` (artifact via `workflow_run`, so a red `main`
  cannot produce one), `Dockerfile` (non-root, no secrets), CODEOWNERS, PR/issue templates,
  `docs/branch-protection.md`, `make usage`.

**PR #2 — `feat(be-02): agent registry with content-addressed input fingerprints`**
(open, 9/9 green, stacked on PR #1)
- `docs/architecture/be-02-agent-registry.md` — proposal; §9 decisions approved.
- `migrations/versions/0002_agent_registry.py` — 3 tables, RLS + policy each, GRANT fix.
- `src/keel/fingerprint.py` — canonicalisation + SHA-256 + secret detection.
- `src/keel/models.py`, `schemas.py`, `api/agents.py`, `main.py` — registry API.
- `tests/test_fingerprint.py` (44 tests), `tests/test_isolation.py` (+11).
- Removed the `X-Org-ID` scaffold; corrected 0001's stale ownership comment.

## 2. Why

Sprint 0 is the cheapest moment to lock the engineering OS. We sell a deploy gate; a repo
where anyone can merge anything teaches the opposite of the product's premise.

## 3. Architecture decisions

- **Registry = stable identity → immutable version → mutable alias.** MLflow, W&B, Langfuse,
  Phoenix, PromptLayer and Humanloop each arrived at this independently. Strongest signal in
  the research; cheap to build; no "buy" avoids it.
- **Rejected MLflow's `stage` enum.** [mlflow#10336](https://github.com/mlflow/mlflow/issues/10336)
  deprecated it for conflating lifecycle + environment + access control. We start split.
- **Fingerprint = canonicalized, behavior-scoped SHA-256.** Not shipped by any surveyed
  vendor. TruLens hashes raw JSON without canonicalizing; Langfuse
  [declined the request](https://github.com/orgs/langfuse/discussions/2161) as "niche". This
  is our IP and it maps straight to the prime directive.
- **Build, don't buy.** Humanloop was acqui-hired and shut down (Sept 2025) with no asset
  transfer; W&B rebuilt its registry within ~2 years. Not putting our differentiator behind
  a vendor with that mortality rate.
- **One required check (`gate`), not eight.** Adding CI jobs must not require editing branch
  protection.

## 4. Security findings

| Finding | Severity | Status |
|---|---|---|
| `GRANT ON ALL TABLES` is a snapshot — any new table is unreadable by `keel_app`. Verified: `permission denied` on a post-migration table. Would fail at **runtime, not CI**. | High (availability) | **Fixed** in PR #2 via `ALTER DEFAULT PRIVILEGES` — fixes the class, not the instance |
| `test_isolation.py` skipped itself when Postgres was absent — a broken CI service container would pass tenant isolation **silently**. | High | **Fixed** in PR #1 (`KEEL_REQUIRE_DB=1`) |
| `middleware.py:21` reads a client-controlled `X-Org-ID` into request state. Authorizes nothing today; tenant-shaped and adjacent to a registry that must not trust one. | Low | Deletion proposed in BE-02 |
| `0001_init.py:60` justifies FORCE RLS with "our app connects as the owner" — false. Wrong premise could lead someone to "fix" the wrong thing. | Low (correctness of reasoning) | Correction proposed |
| Manifests as executable config — [LangSmith GHSA-3644-q5cj-c5c7](https://github.com/langchain-ai/langsmith-sdk/security/advisories/GHSA-3644-q5cj-c5c7): deserializing pulled prompts redirected model traffic + exfiltrated env vars. | Design input | Manifests stored as inert `jsonb`, never deserialized |
| Secrets pasted into a manifest would be stored plaintext **and hashed into an unredactable fingerprint**. | Medium | **Fixed** in PR #2 — rejected at write time *before* hashing; the error names the credential type and never echoes it |
| `X-Org-ID` scaffold removed; `middleware.py` no longer carries a client-controlled tenant value. | Low | **Fixed** in PR #2 |

## 5. Tests performed

Everything below was run, not assumed:

- ruff check + format, `mypy --strict` — clean
- pytest: 11/11 with real Postgres; 6/6 unit without
- `KEEL_REQUIRE_DB` verified **both** directions: skips locally without a DB, fails in CI
- Docker: builds, non-root uid 10001, `/healthz` → `200 {"status":"ok"}`, no `.env` in layers
- terraform fmt + init + validate against the locked provider
- gitleaks: no leaks. pip-audit: confirmed it still exits 1 on a real CVE (installed a
  vulnerable jinja2 to prove it)
- commit-msg hook: 14/14 accept/reject cases; pre-push guard blocks `main`, allows features
- **GRANT snapshot + `ALTER DEFAULT PRIVILEGES` fix — both proven against a live Postgres**
- CI on GitHub: 9/9 green on PR #1; `gate` observed going **red** when security failed, so
  the fan-in genuinely blocks

## 6. Risks

1. **The merge gate is advisory, not enforced.** GitHub restricts branch protection *and*
   rulesets to paid plans on private repos (403). Nothing stops a merge of a red PR or a
   direct push to `main`. Mitigated only by a local, bypassable pre-push hook that cannot
   stop a web-UI merge. **The rule currently lives in one person's discipline.** Revisit
   before a second engineer joins — ~$4/month.
2. **`main` is red right now** (unpinned mypy gained `str-unpack`). PR #1 fixes it; until it
   merges, `main` does not build.
3. **Unbounded tool ranges** caused #2. Now bounded, but the same class of bug applies to
   any unpinned dependency.
4. **Fingerprint canonicalization is the highest-risk code in BE-02.** Too strict → burn
   money re-running evals on whitespace. Too loose → **certify an agent whose behavior
   changed**, i.e. a gate that lies. The second is existential; bias toward including fields.
5. **Work is stacking on an unmerged PR.** `feature/be-02-agent-registry` is based on PR #1.
   The longer it stays open, the more this compounds.

## 7. Next actions

**Blocked on a human — this is the whole queue:**
1. **Merge PR #1** — https://github.com/Nagamiji/agentguard/pull/1 (9/9 green; un-breaks `main`).
2. **Merge PR #2** — https://github.com/Nagamiji/agentguard/pull/2 (9/9 green; stacked on #1,
   retargets to `main` automatically once #1 lands).
3. **Branch protection**: accept advisory, or spend ~$4/mo (decided: advisory, for now).

**Then, unblocked:** BE-03. EVAL-01 is the natural next move — the fingerprint exists
precisely to key an eval cache, and nothing downstream is real until evals run.

## 8. Token usage

See `reports/usage/2026-07.md`. **2026-07-16: 48.9M tokens, ~$38.51** (opus-4-8 + sonnet-5
for the research agent).

Caveat that matters: ccusage reports **per-day, machine-wide** totals from local logs. It
cannot attribute cost to a feature or branch. These numbers label the day; they do not
measure what the merge gate or BE-02 design "cost".
